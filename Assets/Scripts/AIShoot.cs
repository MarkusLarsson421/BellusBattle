using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AIShoot : MonoBehaviour
{
    //Shoot a ray in an even configurable amount around the center of the AI.
    //If a player is hit.
		//Move the weapon closer towards that position.
		//Might require a bit complicated math here.
}
